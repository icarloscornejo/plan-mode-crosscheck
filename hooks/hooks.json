{
  "description": "Plan Mode Crosscheck: on ExitPlanMode, gates showing the plan on an explicit decision (recorded per plan-hash) about an independent Codex CLI audit, offered to the user via the crosscheck skill.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "ExitPlanMode",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/crosscheck.sh",
            "timeout": 15
          }
        ]
      }
    ]
  }
}
